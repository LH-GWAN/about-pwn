# Use-After-Free(UAF) 취약점 분석

## 1. Use-After-Free(UAF)란?
- Use-After-Free(UAF)는 동적으로 할당된 Heap 메모리가 free()를 통해 해제된 이후에도 해당 메모리를 계속 사용하는 메모리 안전성 취약점이다. C/C++과 같이 개발자가 직접 메모리를 관리하는 언어에서 자주 발생하며, 정보 유출, 임의 읽기·쓰기, 권한 상승, 원격 코드 실행(RCE) 등 보안 문제로 이어질 수 있다.
- 프로그램은 일반적으로 malloc()을 통해 Heap 메모리를 할당하고, 사용이 끝난 후 free()를 호출하여 메모리를 반환한다. 만약 free()를 호출하지 않으면 메모리가 계속 점유되어 Memory Leak이 발생하며, 반대로 free() 이후에도 해당 메모리를 계속 참조하면 UAF 취약점이 발생한다.

## 2. Dangling Pointer의 발생 원리
- UAF는 대부분 Dangling Pointer로부터 시작된다.
- 예를 들어 하나의 Heap 객체를 두 개의 포인터가 동시에 참조하고 있다고 가정한다.
```
char *f = malloc(100);
char *g = f;
```
- 이때 f와 g는 모두 동일한 Heap 영역을 가리킨다.
- 이후 ```free(f)를 호출하면 Heap 메모리는 해제된다. 하지만 g는 여전히 같은 주소값을 가지고 있으므로 이미 해제된 메모리를 계속 가리키게 된다.
- 이러한 포인터를 Dangling Pointer라고 한다.
- 중요한 점은 free()는 포인터를 삭제하는 것이 아니라 Heap 메모리를 Heap Allocator에게 반환하는 함수라는 것이다. 따라서 포인터 변수 안에 저장된 주소값은 그대로 남아 있으며, 프로그램은 여전히 해당 주소를 접근할 수 있다고 착각하게 된다.
  
* Heap Allocator: Heap 메모리를 관리하는 컴포넌트. 동일한 크기의 malloc()이 호출되면 Heap Allocator는 성능 향상을 위해 기존에 사용했던 공간을 다시 재사용하는 경우가 많다.

## 3. Heap Grooming
- Heap Grooming은 Heap Allocator의 동작을 예측하여 공격자가 원하는 위치에 원하는 객체를 배치하도록 Heap의 상태를 의도적으로 조작하는 공격 기법이다.
- 정확히 말하면 공격자가 Heap의 배치(Layout)를 설계하는 과정이다.
- Heap이 다음과 같이 존재하면
```
A
B
C
D
```
이러고 
```
free(B)
free(D)
```
를 수행하면
```
A
FREE
C
FREE
```
이 싱태가 된다.
이후에 ```malloc(sizeof(B))``` 을 수행하면 Heap Allocator는 대부분 첫 번째 FREE 공간을 재사용한다.
즉, 
```
A
X
C
FREE
```
이런식이 된다.
- 이러한 성질을 이용하여 공격자는 malloc, free를 수천번 반복하여 Heap의 배치를 원하는 형태로 만든다.

## 4. 공격자가 Heap을 원하는 형태로 만드는 원리
- Heap Allocator는 항상 새로운 메모리를 생성하는 것이 아니라 가능하면 기존에 해제된 공간을 먼저 재사용한다.
- 공격자는 이를 이용하여 다음과 같은 과정을 수행한다.
1. 동일한 크기의 객체를 대량으로 생성
```
A
A
A
A
A
A
A
```
2. 일부만 선택적으로 해제
```
A
FREE
A
FREE
A
FREE
A
```
3. 동일한 크기의 새로운 객체를 생성
- Heap Allocator는 비어있는 공간을 우선적으로 사용하므로
```
A
B
A
B
A
B
A
```
- 와 같이 공격자가 원하는 객체가 특정 위치에 들어가게 된다.
- 이러한 짓을 반복하면 공격자는 각종 정보를 원하는 위치에 넣을 수 있게 된다.

## 5. UAF 공격 과정
- UAF 공격은 일반적으로 다음과 같은 순서로 진행된다.

① Heap 객체 생성

```malloc()```

↓

② 두 개 이상의 포인터가 같은 Heap 객체를 참조

↓

③ 한 포인터만 free()

↓

④ 다른 포인터는 Dangling Pointer가 됨

↓

⑤ Heap Grooming 수행

↓

⑥ 동일한 위치에 공격자가 원하는 객체 생성

↓

⑦ Dangling Pointer가 새 객체를 기존 객체로 착각하고 접근

↓

⑧ 정보 유출 또는 Arbitrary Read/Write 발생

↓

⑨ 권한 상승 또는 RCE

- 즉, UAF의 핵심은 해제된 메모리 자체를 읽는 것이 아니라, 그 위치에 새롭게 생성된 객체를 기존 객체인 것처럼 접근한다는 점이다.

## 6. V8 엔진에서의 UAF 취약점(실전 취약점)
- Google Chrome의 JavaScript 엔진인 V8은 JavaScript 코드를 실행하는 과정에서 다양한 최적화 과정을 수행한다.
- 배열 복사, 정렬, 확장 등의 작업 중에는 내부적으로 현재 배열을 가리키는 포인터를 저장한 상태에서 작업을 진행하는 경우가 있다.
- 공격자는 이러한 과정에서 Getter를 이용하여 프로그램의 실행 흐름을 변경한다.
```
[1, , 3]
```
와 같이 중간에 값이 없는 배열을 Holey Array라고 한다.

- 배열을 읽는 과정에서 해당 위치에 Getter가 정의되어 있으면(아래 코드 참고)
```
Object.defineProperty(Array.prototype, 1, {
    get() {
        ...
    }
});
```
Getter가 자동으로 실행된다.
- 원래 V8은 배열을 순차적으로 처리할 예정이었지만, Getter가 실행되면서 공격자가 원하는 JavaScript 코드가 중간에 수행된다.
```
arr.push(100);
```
와 같은 코드를 실행하면 배열의 크기가 변경된다.
- 기존 배열 공간이 부족하면 V8은 더 큰 Heap 공간을 새로 확보하고 데이터를 복사하는 realloc 을 수행한다.
- 문제는 V8 내부의 일부 코드가 여전히 기존 Elements를 가리키는 포인터를 유지한 채 이후 작업을 계속 수행하는 경우이다.
- 이 포인터는 이미 해제된 Heap을 참조하므로 Dangling Pointer가 되고, 이후 Heap Grooming을 통해 동일한 위치에 새로운 JavaScript 객체를 배치하면 엔진은 이를 기존 배열 객체로 착각하여 접근하게 된다.
- 이로인해 UAF 같은 취약점이 발생하는 것이다.

## 7. 정리
- Use-After-Free(UAF)는 free()로 해제된 Heap 메모리를 프로그램이 계속 사용하는 메모리 안전성 취약점이다. 하나의 Heap 객체를 여러 포인터가 참조하는 상황에서 일부 포인터만 free()를 호출하면 나머지 포인터는 Dangling Pointer가 되어 이미 해제된 메모리를 계속 가리키게 된다. Heap Allocator는 성능 향상을 위해 해제된 메모리를 재사용하며, 공격자는 Heap Grooming을 통해 해당 위치에 원하는 객체를 배치하도록 Heap의 구조를 조작한다. 이후 Dangling Pointer는 새롭게 생성된 객체를 기존 객체로 인식하여 접근하게 되고, 이를 통해 정보 유출, 임의 읽기·쓰기, Type Confusion, 권한 상승, 원격 코드 실행(RCE) 등의 공격으로 이어질 수 있다. Chrome V8 엔진에서는 Holey Array와 Getter를 이용해 배열 처리 중 realloc을 유도하고, 내부 포인터가 적절히 갱신되지 않는 상황을 만들어 UAF를 발생시키는 취약점들이 존재한다. 이러한 사례는 UAF가 단순한 프로그래밍 실수가 아니라, 런타임의 메모리 관리 방식과 객체 재배치 과정이 복합적으로 맞물릴 때 발생하는 대표적인 메모리 안전성 취약점임을 보여준다.
